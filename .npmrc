# Host OOM fix: reduce npm memory, skip heavy browsers on cPanel
fund=false
audit=false
loglevel=warn
prefer-offline=true
# Playwright browsers are huge (400MB) and not needed for homepage; skip on host
# Set PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 in cPanel env if you don't need PDF invoices
